{% set icon = '' %}
{% set header = '' %}
{% for item in pages %}
  {% if item.page == page %}
    {% set icon = item.icon %}
    {% set header = item.header %}
  {% endif %}
{% endfor %}
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <meta name="author" content="pasmurno in llcawc"/>
    <meta name="description" content="{{ metaDescription }}"/>
    <meta name="keywords" content=""/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="robots" content="noindex,nofollow"/>
    <link rel="canonical" href="https://ceterra.ru"/>
    <link rel="shortcut icon" href="/assets/images/favicon.svg" type="image/svg-xml"/>
    <link rel="shortcut icon" href="/assets/images/favicon.ico" type="image/x-icon"/>
    <link
    rel="shortcut icon" href="/assets/images/favicon.png" type="image/png"/>
    <!-- Compressed and components style file: libraries and custom -->
    <link rel="preload" as="style" href="/assets/css/fonts.min.css"/>
    <link rel="stylesheet" href="/assets/css/fonts.min.css" media="print" onload="this.media='all'"/>
    <link
    rel="stylesheet" href="/assets/css/main.min.css">
    <!-- Compressed JavaScript: libraries and custom script -->
    <script defer src="/assets/js/main.min.js"></script>
  </head>
  <body>
    <section class="flex flex-col justify-center min-h-screen">
      {% block header %}
        {% include "../includes/header.njk" %}
      {% endblock %}
      {% block main %}
        This is the default main сontent
      {% endblock %}
      {% block footer %}
        {% include "../includes/footer.njk" %}
      {% endblock %}
    </section>
  </body>
</html>
